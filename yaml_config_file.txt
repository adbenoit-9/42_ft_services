
1) What do you want to create ?
apiVersion: v1 ? apps/v1 ?
kind: Deployement ? Service ?

2) metadata
metadata:
	name: name of the .yaml
	labels:
		app: name_app

3) Specification
specific to the kind (1)
# Deployement
spec:
	replicas:
	selector:
		matchLabels:
			app: name_app
	template:
		template has it's own metadata and spec section applies to pod
		# Pod
		metadata:
			labels:
			app: name_app
		spec:
			containers:
			-	name: name_app
				image:
				ports:
				-	containerPort:
	labels:
		app: name_app
# Service
spec:
	selector:
		app: name_app (defined here !!!!!!! for the connexion between deployement and service)
	ports:
		-	protocol: TCP ?
			port: (DB service -> app Service)
			targetPort: (app Service -> pod = containerPort)

4) Status
Do by K8s => check if spec desired (3) == spec actual

###################

syntax important (aka. indentation)

https://www.youtube.com/watch?v=qmDzcu5uY1I&list=PLy7NrYWoggjwPggqtFsI_zMAwvG0SqYCb&index=20
8min55